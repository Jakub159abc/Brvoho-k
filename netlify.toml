# Statický web – nasazuje se složka netlify-site (NE složka public/).
# Build při každém deployi sestaví netlify-site z site/, článků, kalendáře, Filtrování rostlin/output.html a Duševní příčiny nemocí/index.html.
# Viz NETLIFY-CO-A-JAK.md

[build]
  publish = "netlify-site"
  command = "bash build-netlify.sh"
